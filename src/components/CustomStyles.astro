---
import '@fontsource-variable/inter';
---

<style is:inline>
  /* Light Mode Colors */
  :root {
    --aw-font-sans: 'Inter Variable';
    --aw-font-serif: 'Inter Variable';
    --aw-font-heading: 'Inter Variable';

    /* Black and White Palette */
    --aw-color-primary: rgb(16 16 16); /* Black */
    --aw-color-secondary: rgb(81 81 81); /* Dark Gray */
    --aw-color-accent: rgb(40 40 40); /* Very Dark Gray */

    /* Text and Background */
    --aw-color-text-heading: rgb(0 0 0);
    --aw-color-text-default: rgb(16 16 16);
    --aw-color-text-muted: rgb(16 16 16 / 66%);
    --aw-color-bg-page: rgb(255 255 255); /* White */

    --aw-color-bg-page-dark: rgb(3 6 32);

    ::selection {
      background-color: #e0e0e0;
    }
  }

  /* Dark Mode Colors */
  .dark {
    --aw-font-sans: 'Inter Variable';
    --aw-font-serif: 'Inter Variable';
    --aw-font-heading: 'Inter Variable';

    /* CHANGED: Primary color is now a dark gray for backgrounds */
    --aw-color-primary: rgb(40 40 40); /* Very Dark Gray */
    --aw-color-secondary: rgb(200 200 200); /* Light Gray */
    --aw-color-accent: rgb(229 236 246); /* Very Light Gray */

    /* Text and Background */
    --aw-color-text-heading: rgb(247, 248, 248);
    --aw-color-text-default: rgb(229 236 246);
    --aw-color-text-muted: rgb(229 236 246 / 66%);
    --aw-color-bg-page: rgb(16 16 16); /* Black */

    ::selection {
      background-color: #444444;
      color: snow;
    }
  }
</style>
